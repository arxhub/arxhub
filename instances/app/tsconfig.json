{
  "extends": "@arxhub/toolchain-tsconfig",
  "compilerOptions": {
    /* The base names no runtime; this bundle is a browser one. */
    "lib": ["esnext", "DOM", "DOM.Iterable"],
    "jsx": "preserve"
  },
  "include": ["src/**/*.ts", "src/**/*.d.ts", "src/**/*.tsx", "src/**/*.vue"]
}
