// The id this plugin registers its type under, in a module of its own so a page or a test can name it
// without importing the plugin that registers it.
export const PUBLISH_TYPE_ID = 'arxhub.publish'
